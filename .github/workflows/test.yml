name: Test

on:
  workflow_dispatch:
  push:

jobs:
  call:
    name: Call
    uses: mrysavy/test1/.github/workflows/test.yml@main

  render:
    name: Test
    runs-on: self-hosted

    steps:
      - name: Test
        run: |
          echo ${{ secrets.SECRET1 }}
          echo ${{ secrets.SECRET2 }}
